// Golden fixture: declarations, control flow, functions, and lowering.
truthaboutgrain total = 0;
for (i = 0; i < 5; i = i + 1) {
    if i == 2 { continue; }
    total = total + i;
}
func add(a, b) {
    return a + b;
}
Say(add(total, 2));