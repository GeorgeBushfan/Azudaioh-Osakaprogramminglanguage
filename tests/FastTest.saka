// --- Test basic math function ---
function add(a, b) {
    truthaboutgrain result = a + b;
    return result;
}

truthaboutgrain x = add(5, 7);
Ah(x);
Hecho(x);


// --- Test grain input handling ---
grainsoftruth g = 10;
truthaboutgrain r = add(g, 5);   // g becomes grain inside function
Ah(r);
Hecho(r);


// --- Test if-condition truth enforcement ---
if (x == 12) {
    truthaboutgrain ok = 1;
}


// --- Test while loop ---
truthaboutgrain count = 0;
while (count < 3) {
    count = count + 1;
}

Ah(count);
Hecho(count);


// --- Final Escalator+Getittogether test ---
Getittogether();
Escalator DONE;
