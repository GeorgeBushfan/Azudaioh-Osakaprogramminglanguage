[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "OsakaProgrammingLanguage"
version = "1.1.0"
description = "Osaka Programming Language CLI (interpreter + VM + equivalence lock)"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "GeorgeBushfan - James Alexander" }
]
dependencies = []

[project.optional-dependencies]
dev = [
  "pyinstaller>=6.0",
]

[project.scripts]
osaka = "saka:main"

[tool.setuptools]
py-modules = [
  "arg_parser",
  "ast_nodes",
  "bytecode",
  "builtin_registry",
  "bootstrap_profile",
  "compiler",
  "equiv_lock",
  "equiv_test",
  "frontend_contract",
  "interpreter",
  "lexer",
  "parser",
  "print_bytecode",
  "run_tests",
  "run_vm",
  "runtime",
  "saka",
  "sbc",
  "selfhost_bundle",
  "selfhost_harness",
  "verifier",
  "verify_tests",
  "vm",
]

[tool.setuptools.packages.find]
where = ["."]
exclude = ["tests*"]